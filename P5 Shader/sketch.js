function setup() {
    // write your code for setup function inside these curly brackets
    createCanvas(400, 400);
}

function draw() {
    //draw function is where the real magic happens.
    background(220);
}