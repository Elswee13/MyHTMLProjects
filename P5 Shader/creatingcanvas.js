function setup() {
    createCanvas(800, 300);
    background(220, 220, 220);
    //using rectMode to positioning
    rectMode(CENTER);
}

function draw() {
    //drawing a rectangle
    rect(50, 100, 200, 100);
    rect(400, 150, 100, 100);
}